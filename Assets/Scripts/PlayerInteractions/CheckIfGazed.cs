using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Gameobjects that can be interacted with player gaze should check if it's gazed from here
/// </summary>
public class CheckIfGazed : MonoBehaviour
{
    public bool isBeingGazed; // If the object that this gaze detector attached to is currently being gazed by the player

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
