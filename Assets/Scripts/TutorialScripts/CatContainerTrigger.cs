using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CatContainerTrigger : MonoBehaviour
{
    public bool isShot; // Has the container been shot

    // Use this for initialization
    void Start()
    {
        isShot = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
